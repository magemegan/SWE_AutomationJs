# Restaurant Database Package

## Structure

- `sql/` = schema, seed data, triggers, views, QA queries
- `scripts/` = Python runners
- `data/` = SQLite database file

## Setup

From the `Database` folder:

```bash
chmod +x run_all.sh
./run_all.sh